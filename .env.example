# =============================================================================
# CONTAINER HUB PLUS - ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables required for Container Hub Plus.
# Copy this file to .env and fill in your actual values.
# 
# Setup Process:
# 1. Docker Configuration - Configure Docker socket connection
# 2. Authentication - NextAuth URL and secret key
# 3. OAuth Providers - Google, GitHub, GitLab (optional)
# 4. Database - Supabase connection and API keys
# 5. Application - App URL and admin users
# 6. Logging - Log levels and preferences
# =============================================================================

# =============================================================================
# 1. DOCKER CONFIGURATION
# =============================================================================
# Docker socket path for container management
# Windows: //./pipe/docker_engine
# Linux/macOS: /var/run/docker.sock
DOCKER_SOCKET=//./pipe/docker_engine

# =============================================================================
# 2. AUTHENTICATION CONFIGURATION
# =============================================================================
# NextAuth.js configuration for user authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=execute this -> npm run generate-secret

# =============================================================================
# 3. OAUTH PROVIDERS (Optional)
# =============================================================================
# Configure OAuth providers for user authentication
# These can also be configured via the Settings UI after initial setup

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth (for Git integration and building images from repositories)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:3000/api/git/callback/github

# GitLab OAuth (for Git integration and building images from repositories)
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=
GITLAB_CALLBACK_URL=http://localhost:3000/api/git/callback/gitlab

# =============================================================================
# 4. DATABASE CONFIGURATION
# =============================================================================
# Supabase PostgreSQL database configuration
# Get these values from: https://supabase.com/dashboard -> Your Project -> Settings -> API

# PostgreSQL connection string
# Format: postgresql://postgres:PASSWORD@PROJECT_REF.supabase.co:5432/postgres
# Note: URL encode special characters in password (e.g., $X2FX*YHP?!Uq9M → %24X2FX%2AYHP%3F%21Uq9M)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_PROJECT.supabase.co:5432/postgres

# Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co

# Supabase anonymous key (for client-side operations)
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Supabase service role key (for server-side operations, bypasses RLS)
# Use either the legacy "service_role" key OR the new "secret key" format
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
# Alternative new format:
# SUPABASE_SECRET_KEY=YOUR_SECRET_KEY

# =============================================================================
# 5. APPLICATION CONFIGURATION
# =============================================================================
# Application URL (must match your deployment URL)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin email addresses (comma-separated list)
# These users will have administrative privileges
ADMIN_EMAILS=your.email@example.com

# =============================================================================
# 6. LOGGING CONFIGURATION
# =============================================================================
# Logging preferences and settings

# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Enable/disable specific logging features
ENABLE_REQUEST_LOGGING=true
ENABLE_PERFORMANCE_LOGGING=true
ENABLE_DOCKER_LOGGING=true

# Log format: pretty (human readable) or json (machine readable)
LOG_FORMAT=pretty

# Maximum log file size in bytes (default: 10MB)
MAX_LOG_SIZE=10485760

# Log retention period in days
LOG_RETENTION_DAYS=30

# =============================================================================
# SETUP NOTES
# =============================================================================
# 
# OAuth Migration:
# - OAuth providers can now be configured via the Settings UI
# - .env configuration is still supported for quick setup
# - See docs/OAUTH_MIGRATION.md for migration details
#
# Security:
# - Keep your .env file secure and never commit it to version control
# - Use strong, randomly generated secrets
# - In production, use HTTPS URLs for all external services
#
# Getting Started:
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. Run the setup wizard at /setup for guided configuration
# 4. Or configure manually using the values above
# =============================================================================
